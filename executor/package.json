{
  "name": "gurukul-executor",
  "version": "1.0.0",
  "private": true,
  "description": "Shared prepare-and-approve browser executor for Gurukul.",
  "type": "commonjs",
  "scripts": { "start": "node src/server.js", "test": "node --test test/*.test.js" },
  "dependencies": { "express": "^5.1.0", "playwright": "^1.55.0" },
  "engines": { "node": ">=20" }
}
