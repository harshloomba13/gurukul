# python package requirements file

# Note to our Learners: Our courses are created at a specific point in time.
# The models and libraries used will naturally change over time.
# If you download and run the notebooks locally, you should create a Python
# virtual environment with the Python version matching the one used in the
# course and use this requirements.txt file to install the required Python
# packages.
# Due to the wide diversity of platforms and environments, DeepLearning does
# not explicitly support running locally. However, if you encounter issues,
# the course support channel is a good place to ask questions.

# Python version used locally while preparing this file: 3.12.3

# Pip-installable dependencies used by this lesson.
matplotlib
numpy
pandas
torch
transformers
python-dotenv
accelerate

# Standard library modules like typing, copy, random, and time do not belong
# in requirements.txt because Python already includes them.
